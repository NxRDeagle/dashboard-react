// Remote type declarations are not needed for webpack-bundled import()
// because GreetingWidget loads via @module-federation/runtime's loadRemote API.
// Add entries here if additional static remotes are declared in module-federation.config.ts.
export {};
